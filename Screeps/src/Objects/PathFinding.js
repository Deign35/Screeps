// Need to write my own pathfinding :(

// Need to detect existing creeps.  Memory gets orphaned, and if a manual reset is executed, I lose
// everything and those creeps become pointless.

// What about hivemind stages
// I.e. Room[sim].hiveMind = new HiveMind(HiveMindType_Enum.Bootstrapper);
//      Room[sim].hiveMind = new HiveMind(HiveMindType_Enum.Intermediate);
//      Room[sim].hiveMind = new HiveMind(HiveMindType_Enum.CompleteRoom);